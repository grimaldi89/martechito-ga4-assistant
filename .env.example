MODEL=gpt-5.5 #Open AI model to use. Must support the Responses API web_search tool (e.g. gpt-5.5, gpt-4.1). Default: gpt-5.5

# OPENAI_API_KEY is NOT set here: each user enters their own key directly in the
# Streamlit sidebar at runtime (kept only in their browser session, never persisted).
